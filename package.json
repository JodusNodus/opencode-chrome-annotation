{
  "name": "opencode-chrome-annotation",
  "version": "1.0.3",
  "description": "Annotate pages in Chrome and send context into your local OpenCode setup.",
  "type": "module",
  "main": "./dist/plugin.js",
  "exports": {
    ".": "./dist/plugin.js",
    "./plugin": "./dist/plugin.js",
    "./tui": "./dist/tui.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "bun build src/plugin.ts src/tui.tsx --target=node --external @opencode-ai/plugin --external @opencode-ai/plugin/tui --external @opentui/core --external @opentui/solid --external solid-js --outdir=dist --entry-naming='[name].js'",
    "build:extension": "node scripts/build-extension.mjs",
    "test": "bun test",
    "test:integration:v2": "node scripts/test-v2-integration.mjs",
    "typecheck": "tsc --noEmit",
    "check:extension": "npm run build:extension && node --check extension/background.js && node --check extension/injected/dom.js && node -e \"JSON.parse(require('fs').readFileSync('extension/manifest.json','utf8'))\"",
    "dev": "bun run build",
    "dev:setup": "bun run build",
    "build:zip": "node scripts/build-extension-zip.mjs",
    "icons:generate": "sips -s format png -z 16 16 icon.svg --out extension/icons/icon16.png && sips -s format png -z 48 48 icon.svg --out extension/icons/icon48.png && sips -s format png -z 128 128 icon.svg --out extension/icons/icon128.png",
    "prepublishOnly": "bun run build",
    "publish": "node -e \"if (process.env.npm_command === 'publish') process.exit(0); require('child_process').execSync('npm publish --access public', { stdio: 'inherit' });\""
  },
  "keywords": [
    "opencode",
    "browser",
    "annotation",
    "chrome",
    "plugin",
    "localhost"
  ],
  "author": "Benjamin Shafii",
  "license": "GPL-3.0-only",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jodusnodus/opencode-chrome-annotation.git"
  },
  "bugs": {
    "url": "https://github.com/jodusnodus/opencode-chrome-annotation/issues"
  },
  "homepage": "https://github.com/jodusnodus/opencode-chrome-annotation#readme",
  "overrides": {
    "@babel/core": "7.29.7"
  },
  "dependencies": {
    "@opencode-ai/plugin": "0.0.0-beta-17639",
    "@opentui/core": "0.5.4",
    "solid-js": "1.9.12"
  },
  "devDependencies": {
    "@opentui/solid": "0.5.4",
    "@types/chrome": "*",
    "bun-types": "*",
    "typescript": "*"
  }
}
